# https://taskfile.dev

version: "3"

tasks:
  default:
    cmds:
      - go run ./... {{.CLI_ARGS}}

  deps:
    cmds:
      - go mod tidy

  build:
    cmds:
      - go build -o dist/loops

  test:
    cmds:
      - go test ./...
